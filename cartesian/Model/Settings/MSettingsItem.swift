import UIKit

class MSettingsItem
{
    let cellHeight:CGFloat
    let reusableIdentifier:String
    
    init(cellHeight:CGFloat, reusableIdentifier:String)
    {
        self.cellHeight = cellHeight
        self.reusableIdentifier = reusableIdentifier
    }
}
